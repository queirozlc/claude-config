---
alwaysApply: true
---

When working with libraries, frameworks, or APIs, fetch current documentation with the Context7 MCP instead of relying on training data. This covers setup, code generation, API references, and anything tied to a specific package. Prefer an exact name and a version-specific ID when the user names a version. Cite the version you read.
